import { StaticImageData } from "next/image";

export type ServiceCardProps = {
  src: any;
  text: string;
  className?: string;
  url: string;
};
export type CardBodyProps = {
  text: string;
  url: string;
};
